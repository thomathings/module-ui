{
  "name": "@thomathings/module-ui-css",
  "version": "0.0.1",
  "description": "CSS for Module UI.",
  "license": "MIT",
  "files": [
    "dist",
    "../../LICENSE"
  ],
  "scripts": {
    "build": "scripts/build",
    "build:css": "vite build",
    "build:types": "typed-scss-modules src",
    "watch": "run-p watch:*",
    "watch:types": "typed-scss-modules src -w",
    "test": "echo 'test'"
  }
}