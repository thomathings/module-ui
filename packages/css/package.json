{
  "name": "@thomathings/module-ui-css",
  "version": "0.0.1",
  "description": "CSS for Module UI.",
  "license": "MIT",
  "files": [
    "components/*.module.css",
    "components/*.module.css.d.ts",
    "../../LICENSE"
  ],
  "scripts": {
    "dev": "run-p dev:*",
    "dev:css": "vite build -w",
    "dev:types": "pnpm generate:types -w",
    "build": "scripts/build",
    "build:css": "vite build",
    "generate": "run-p watch:*",
    "generate:types": "tcm components",
    "test": "echo 'test'"
  }
}